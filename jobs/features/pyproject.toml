
[project]
name = "ppml-job-features"
version = "0.1.0"
description = "Batch feature engineering job for the Player Prop ML Analysis Platform"
requires-python = ">=3.11"
dependencies = []

[tool.ruff]
