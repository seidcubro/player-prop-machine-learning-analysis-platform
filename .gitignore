# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.python-version
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/

# Node
node_modules/
dist/
build/
coverage/
*.tsbuildinfo

# Env files
.env
.env.*
!.env.example
# The dev server's own origin, which is localhost and is not a secret. It is
# committed so a fresh clone renders the Open Graph tags with a real value
# instead of leaving the build placeholder in the markup.
!apps/web/.env.development

# Logs
*.log
logs/

# Docker
*.pid

# Data / artifacts
artifacts/
tmp/
temp/
.cache/

# Jupyter
.ipynb_checkpoints/

# Terraform
.terraform/
*.tfstate
*.tfstate.*
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Misc
*.bak
*.tmp

# Database dumps from deploy/migrate_db.sh. 55MB of the Odds history is not
# something to keep in git, and it is regenerated on demand.
*.dump

# Generated brand assets from scripts/build_brand.py. Build output, not source.
brand/
