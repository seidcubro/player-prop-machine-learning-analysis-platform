# Local defaults, committed on purpose.
#
# These are the values a dev server needs and nothing secret is in them. The
# production values live in the Vercel project settings, where the build reads
# them, and the build refuses to run if either is missing.
VITE_SITE_URL=http://localhost:5173
